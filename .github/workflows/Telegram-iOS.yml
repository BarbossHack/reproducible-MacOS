name: Telegram-iOS

on:
  push:

jobs:
  applestore-verify:
    runs-on: macos-26
    steps:
      - name: build
        run: |
          set -x

          ls /Applications/Xcode.app/Contents/Developer/Toolchains/XcodeDefault.xctoolchain/

          cat /Applications/Xcode.app/Contents/Developer/Toolchains/XcodeDefault.xctoolchain/ToolchainInfo.plist