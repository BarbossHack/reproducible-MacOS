name: Telegram-iOS

on:
  push:

jobs:
  applestore-verify:
    runs-on: macos-26
    steps:
      - name: Install mitmproxy
        run: |
          brew install xcodesorg/made/xcodes

          xcodes --help

          ls -lsah /Applications/
